# values.yaml
ingressRoute:
  dashboard:
    enabled: true
    matchRule: Host(`traefik.laniakea.local`)
    entryPoints:
      - web
providers:
  kubernetesGateway:
    enabled: true
gateway:
  listeners:
    web:
      namespacePolicy:
        from: All
